name: heat_stress_monitor
description: HSE Heat Stress App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.0.4
  google_mlkit_text_recognition: ^0.11.0  # <-- This is the "read pic" engine
  intl: ^0.18.0                            # For date formatting

flutter:
  uses-material-design: true